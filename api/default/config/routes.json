{
  "routes": [
    {
      "method": "GET",
      "path": "/defaults",
      "handler": "default.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/defaults/count",
      "handler": "default.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/defaults/:id",
      "handler": "default.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/defaults",
      "handler": "default.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/defaults/:id",
      "handler": "default.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/defaults/:id",
      "handler": "default.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
