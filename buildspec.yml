version: 0.2

env:
  variables:
    NODE_ENV: "production"

phases:
  install:
    runtime-versions:
      nodejs: 13
    commands:
      - yarn install
  build:
    commands:
      - yarn build
      - cd ..
      - cp -rf ./build/* ./public/
      - yarn build
artifacts:
  files:
    - '**/*'